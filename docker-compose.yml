services:
  frigate-gotify:
    image: ghcr.io/tomatomonster69/frigate-gotify:main
    container_name: frigate-gotify
    restart: unless-stopped
    env_file:
      - .env
    environment:
      # Override any env vars here if needed
      - TZ=Europe/Berlin
    ports:
      - "80:80"
    networks:
      - frigate-network
    volumes:
      - ./config:/app/config
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  frigate-network:
    driver: bridge
    # Network is automatically created by docker-compose
